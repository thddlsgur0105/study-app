mixin roomPreview(room)
    a(href=`/rooms/${room._id}`).preview-card 
        span=room.title
        .room__description 
            span.room__people=`✍🏼: ${room.study.members.length}명`
            span.room__watched=`👀: ${room.meta.views}명`