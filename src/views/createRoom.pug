extends base.pug

block content 
    form(method="POST")
        input(type="text", placeholder="제목", name="title", required)
        input(type="text", placeholder="해시태그", name="hashtags")
        textarea(placeholder="내용을 입력하세요.", cols="30", rows="10", name="content", required)
        input(type="submit", value="스터디 생성")